# Python (Backend)
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.cache/
data/  # if this is generated or contains temp data (optional)

# Node.js (Frontend)
frontend/node_modules/
frontend/.cache/
frontend/dist/
frontend/build/  # Optional: Keep if you want to rebuild on deploy, ignore if not

# OS / IDE Files
.DS_Store
Thumbs.db
.vscode/
.idea/

# Environment Files
.env

# Logs
*.log

# Optional: Ignore frontend build locally but include in deployment if needed
# If you serve frontend via backend, you may choose to keep `frontend/build/` committed
# If you deploy frontend separately, it's better to ignore `frontend/build/`
